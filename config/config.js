var config = {
    paths: {
        modules : ""
    },
	modules: [
		{
			module: "light-switch",
            config: {
                
            }
		}
    ]
};

if (typeof module !== 'undefined') {module.exports = config;}